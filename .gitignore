# Dependencies and build artefacts
node_modules/
__pycache__/

# SingleFile snapshots of Instagram pages.
# verify.py and scoped.py read this folder, so a fresh clone
# cannot run either script until snapshots are captured.
Instagram_snapshots/

# Claude Code's local configuration and skills.
# Machine-specific, so a clone does not get the shipping-a-release checklist;
# CLAUDE.md records that it lives here and points at the last tracked copy.
.claude/
